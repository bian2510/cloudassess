<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Organisation</th>
      <th>Location</th>
      <th>Record count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
        <tr>
          <td><%= @person.first_name %></td>
          <td><%= @person.last_name %></td>
          <td><%= @person.organisation.title %></td>
          <td><%= @person.organisation.location %></td>
          <td><%= @person.record_count %></td>
        </tr>
  </tbody>
</table>
<%= link_to 'Edit', edit_person_path(@person) %>
<%= link_to 'Back', people_path %>